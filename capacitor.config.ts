import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sentry-capacitor-breadcrumb-characters-issue',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
